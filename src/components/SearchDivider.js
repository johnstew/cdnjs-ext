import React, { PropTypes } from 'react';
import * as styles from '../global/styles';

const SearchDivider = (props) => {
  return (
    <div style={styles.search.divider} />
  );
}

export default SearchDivider;
