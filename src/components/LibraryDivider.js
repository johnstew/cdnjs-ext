import React, { PropTypes } from 'react';
import * as styles from '../global/styles';

const LibraryDivider = (props) => {
  return (
    <div style={styles.results.libraryDivider} />
  )
};

export default LibraryDivider;
